import { createStore } from 'vuex'

const state = {}
const getters = {}
const actions = {}
const mutations = {}

const store = new createStore({
    state,
    getters,
    actions,
    mutations
})

export default store;