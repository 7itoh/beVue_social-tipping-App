import { createStore } from 'vuex'

const state = {}
const getters = {}
const mutations = {}
const actions = {}

const store = new createStore({
    state,
    getters,
    mutations,
    actions,
})

export default store;